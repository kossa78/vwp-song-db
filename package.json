import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "V.W.P Song DB",
  description: "Virtual Witch Phenomenon Database",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
